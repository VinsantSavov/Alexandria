@using Alexandria.Web.ViewModels.Users
@model UsersReviewViewModel

<div class="container">
    <div class="row">
        <div class="col-md-1"></div>
        <div class="col-md-9">
            <h4 class="mb-3">
                <a class="text-dark"
                   asp-controller="Users"
                   asp-action="Details"
                   asp-route-id="@Model.Id">
                    @Model.Username's
                </a>
                all book reviews
            </h4>
            @if (Model.AllReviews.Count() == 0)
            {
                <p class="text-center">@Model.Username has no reviews.</p>
            }
            else
            {
                <table class="table">
                    <thead>
                        <tr>
                            <th scope="col">Cover</th>
                            <th scope="col">Title</th>
                            <th scope="col">Author</th>
                            <th scope="col">Review</th>
                            <th scope="col">Created</th>
                            <th scope="col"></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var review in Model.AllReviews)
                        {
                            <partial name="_ReviewsPartial" model="review" />
                        }
                    </tbody>
                </table>
                <div class="my-3">
                    <partial name="_PaginationPartialWithId" />
                </div>
            }
        </div>
    </div>
</div>